# Phase 2G Functional & Interaction Matrix

## Overview
This matrix comprehensively documents all interactive controls, application callbacks, architectural pathways, expected device and UI behaviors, and testing status across the Keeva application for Phase 2G End-to-End Functional Validation.

---

## Interactive Controls Matrix

| Screen | Control | Expected Action | Application Callback | Provider / Notifier | Use Case | Repository / Platform Path | Expected Device Result | Expected UI Result | Test Status | Evidence |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :---: | :--- |
| **Onboarding** | `PrimaryButton` ("Connect Folder") | Request SAF directory permission | `onPressed` -> `AccessNotifier.requestAccess()` | `accessNotifierProvider` | `RequestStorageAccessUseCase` | `StatusRepository.requestAccess()` -> `StatusPlatformDatasource.requestFolderAccess()` -> `MethodChannelStatusScanner` -> `AndroidStatusScanner.kt` | Launches Android system document tree picker (`ACTION_OPEN_DOCUMENT_TREE`) | On permission grant, transitions to `AppShell` with Moments; on denial, displays calm error state with retry CTA | **PASS** | `saf_picker.png`, `grant_dialog.png`, `after_grant.png` |
| **Onboarding** | Privacy link in `PermissionGuide` | View privacy and local-first commitments | `onTap` -> `KeevaBottomSheet.showTrustDetails` | N/A (Presentation) | N/A | Local UI component | None (local display) | Smooth modal bottom sheet slides up with 24dp rounded corners, drag handle, and local-first details | **PASS** | `settings_privacy_sheet.png`, 206 unit/widget tests |
| **Onboarding** | Error CTA ("Try Again" / "Connect Folder") | Re-trigger permission request after cancellation or denial | `onAction` -> `AccessNotifier.requestAccess()` | `accessNotifierProvider` | `RequestStorageAccessUseCase` | Same as initial SAF request | Android SAF picker re-opens | Error disappears, loading indicator briefly appears, picker opens | **PASS** | `saf_picker_reconnect.png`, `returned_settings.png` |
| **Shell / Nav** | Bottom Navigation Bar ("Moments" tab) | Switch active destination to Moments | `onDestinationSelected(NavDestination.moments)` | N/A (`KeevaApp` state) | N/A | In-memory navigation state | `HapticFeedback.selectionClick()` triggered | Navigation bar indicator shifts to Moments, `MomentsScreen` rendered | **PASS** | `app_navigation_test.dart`, `keeva_moments.png` |
| **Shell / Nav** | Bottom Navigation Bar ("Kept" tab) | Switch active destination to Kept Media Vault | `onDestinationSelected(NavDestination.kept)` | N/A (`KeevaApp` state) | N/A | In-memory navigation state | `HapticFeedback.selectionClick()` triggered | Navigation bar indicator shifts to Kept, `KeptVaultScreen` rendered | **PASS** | `app_navigation_test.dart`, `kept_vault_populated.png` |
| **Shell / Nav** | Bottom Navigation Bar ("Settings" tab) | Switch active destination to Settings | `onDestinationSelected(NavDestination.settings)` | N/A (`KeevaApp` state) | N/A | In-memory navigation state | `HapticFeedback.selectionClick()` triggered | Navigation bar indicator shifts to Settings, `SettingsScreen` rendered | **PASS** | `app_navigation_test.dart`, `settings_screen.png` |
| **Moments** | Top Bar Brand Privacy Shield | Open Privacy & Trust Bottom Sheet | `onTap` -> `KeevaBottomSheet.showTrustDetails(context)` | N/A (Presentation) | N/A | Local UI component | Haptic feedback triggered | Modal bottom sheet slides up showing 100% on-device guarantee | **PASS** | `settings_privacy_sheet.png`, widget tests |
| **Moments** | `RefreshIndicator` (Pull-to-refresh) | Scan WhatsApp status folder for fresh media | `onRefresh` -> `StatusListNotifier.refresh()` | `statusListNotifierProvider` | `GetStatusesUseCase` | `StatusRepository.getStatuses()` -> `StatusPlatformDatasource.scanStatuses()` -> `MethodChannelStatusScanner` -> `AndroidStatusScanner.kt` | Native ContentResolver queries `.Statuses` tree directory | Mint circular spinner animates, grid silently reloads fresh items, dismisses on complete | **PASS** | `keeva_moments.png`, real device pull-to-refresh |
| **Moments** | `FilterControl` ("All" chip) | Filter grid to show all moments | `onFilterChanged(MediaFilter.all)` | Local widget state | N/A | In-memory filter on `statusListState.items` | None | All items shown, chip pill highlights with `darkPrimaryContainer` | **PASS** | `keeva_moments.png` (5 items) |
| **Moments** | `FilterControl` ("Photos" chip) | Filter grid to show only photo moments | `onFilterChanged(MediaFilter.photos)` | Local widget state | N/A | In-memory filter where `!item.isVideo` | None | Only photos shown, count matches, chip pill highlights | **PASS** | `filter_photos.png` (2 photos) |
| **Moments** | `FilterControl` ("Videos" chip) | Filter grid to show only video moments | `onFilterChanged(MediaFilter.videos)` | Local widget state | N/A | In-memory filter where `item.isVideo` | None | Only videos shown with video badges, count matches | **PASS** | `filter_videos.png` (3 videos) |
| **Moments** | `StatusCard` tap (Photo) | Open fullscreen Media Viewer | `onTap` -> `_openViewer(item)` | N/A (`KeevaApp` navigation) | `GetThumbnailUseCase` | Resolves thumbnail cache | None | Seamless 300ms Hero transition into edge-to-edge `MediaViewerScreen` | **PASS** | `viewer.png`, `viewer_flow_test.dart` |
| **Moments** | `StatusCard` tap (Video) | Open fullscreen Media Viewer and prepare video | `onTap` -> `_openViewer(item)` | `viewerNotifierProvider` | `PrepareVideoPlaybackUseCase` | `StatusRepository.prepareVideo()` -> `AndroidStatusScanner.prepareVideo()` -> `VideoCacheManager` | Native video file cached in app cache dir | Hero transition into viewer, video prepares in background | **PASS** | `viewer.png`, real video playback |
| **Moments** | `KeepButton` overlay (Unsaved item) | Save moment to system public gallery | `onPressed` -> `SaveNotifier.save(item)` | `saveNotifierProvider` | `SaveStatusUseCase` | `StatusRepository.saveStatus()` -> `StatusPlatformDatasource.saveStatus()` -> `MediaStoreSaver.kt` | Real media file written to `Pictures/SavedStatus/` via MediaStore API | KeepButton: Idle -> Saving (spinner) -> Success (Checkmark + Aura Wave). Toast: "Saved to gallery" | **PASS** | Verified files `/sdcard/Pictures/SavedStatus/` |
| **Moments** | `KeepButton` overlay (Rapid double-tap) | Debounce rapid taps to prevent duplicate saves | `_handleTapDown` / `_handleTap` (400ms debounce) | `saveNotifierProvider` | `SaveStatusUseCase` | Blocked at button and notifier level | Exactly ONE save operation performed on device | Single haptic, single spinner, single file created | **PASS** | Verified single file on disk |
| **Moments** | `KeepButton` overlay (Already kept) | Provide feedback and options modal without duplicate save | `_handleTap` -> Haptic + Options Modal | `saveNotifierProvider` | N/A (Safeguard) | No duplicate native insert | `HapticFeedback.selectionClick()` triggered | Shake nudge on button, options modal: "View in Kept Vault" or "Save Copy" | **PASS** | `already_kept_modal_open.png` |
| **Moments** | Error State ("Try Again") | Retry loading statuses after a scan failure | `onRetry` -> `StatusListNotifier.load()` | `statusListNotifierProvider` | `GetStatusesUseCase` | `StatusRepository.getStatuses()` -> Native scanner | Native ContentResolver scan re-executed | Shimmer loading cards display, then refreshed grid or calm error | **PASS** | Unit + integration error state tests |
| **Viewer** | Fullscreen Canvas tap | Toggle visibility of top and bottom chrome | `_toggleChrome()` | Local widget state | N/A | None | `HapticFeedback.selectionClick()` triggered | Top bar and bottom floating pill animate offscreen / back onscreen (240ms) | **PASS** | `viewer_flow_test.dart` |
| **Viewer** | Vertical drag (>= 120dp or >= 800dp/s) | Interactive swipe-to-dismiss viewer | `_handleVerticalDragEnd()` | N/A (Navigator) | N/A | None | None | Canvas scales down to 0.85 and translates with drag, smoothly dismisses to Moments | **PASS** | Real device swipe gestures |
| **Viewer** | Vertical drag (< 120dp and < 800dp/s) | Snap-back to center on partial drag | `_snapBackController.forward()` | Local widget state | N/A | None | None | Canvas springs back to center (0, 0) with `AppMotion.buttonSpring` | **PASS** | Real device partial drag |
| **Viewer** | Top Bar Back Button (`IconButton`) | Dismiss viewer and return to originating screen | `onPressed` -> `Navigator.maybePop()` | N/A (Navigator) | N/A | None | None | Viewer exits via reverse route transition (250ms), source screen restored | **PASS** | `after_viewer_back.png`, `viewer_flow_test.dart` |
| **Viewer** | Top Bar Info Button (`IconButton`) | Open Media Information bottom sheet | `_showMediaDetailsSheet(context)` | Local widget state | N/A | None | `HapticFeedback.lightImpact()` triggered | Modal sheet displays real file name, media type, size in MB, modification date, SAF doc ID | **PASS** | `media_info.png`, `viewer_flow_test.dart` |
| **Viewer** | Bottom Pill "Share" Button | Share media attachment via native Android share sheet | `onShare(item)` | `shareNotifierProvider` / `ShareUseCase` | `ShareStatusUseCase` | Native Intent Chooser via `FileProvider` (`ACTION_SEND`) | System Android Share Sheet opens with valid `content://` URI and proper MIME type | Native share sheet displays with target apps (Files, Gmail, WhatsApp, etc.) | **PASS** | `share_sheet.png`, `after_share_cancel.png` |
| **Viewer** | Bottom Pill `KeepButton` (Unsaved item) | Save moment to system public gallery | `onPressed` -> `SaveNotifier.save(item)` | `saveNotifierProvider` | `SaveStatusUseCase` | `StatusRepository.saveStatus()` -> `MediaStoreSaver.kt` | Media file written to `Pictures/SavedStatus/` via MediaStore | Button transitions to "Kept ✓" pill with mint checkmark, toast displayed | **PASS** | Verified file on device filesystem |
| **Viewer** | Bottom Pill `KeepButton` (Already kept) | Feedback / options modal | `onPressed` | N/A | N/A | None | `HapticFeedback.selectionClick()` triggered | Options modal ("View in Kept Vault", "Save Copy") | **PASS** | Real device tap test |
| **Viewer** | Bottom Pill Info Button (`IconButton`) | Open Media Information bottom sheet | `_showMediaDetailsSheet(context)` | Local widget state | N/A | None | `HapticFeedback.lightImpact()` triggered | Modal sheet displays full media metadata | **PASS** | `media_info.png` |
| **Kept Vault** | `FilterControl` (All / Photos / Videos) | Filter vault items by media type | `onFilterChanged(filter)` | Local widget state | N/A | In-memory filter on kept items | None | Grid filters immediately, storage metrics update | **PASS** | `kept_vault_photos.png`, `kept_vault_videos.png` |
| **Kept Vault** | Kept Status Card tap | Open fullscreen viewer for preserved media | `onTap` -> `_openViewer(item)` | N/A (Navigator) | `GetThumbnailUseCase` | Resolves thumbnail cache | None | Hero transition into fullscreen viewer | **PASS** | `viewer_from_vault.png` |
| **Kept Vault** | Empty State CTA ("Explore moments") | Navigate back to Moments screen | `onEmptyAction` -> `setState(activeDestination = moments)` | N/A (`KeevaApp` state) | N/A | In-memory destination change | `HapticFeedback.selectionClick()` triggered | Active tab switches to Moments, grid displayed | **PASS** | Tested in empty vault state |
| **Settings** | `SecondaryButton` ("Reconnect Folder") | Re-trigger SAF picker to select/switch folder | `onPressed` -> `AccessNotifier.requestAccess()` | `accessNotifierProvider` | `RequestStorageAccessUseCase` | `StatusRepository.requestAccess()` -> Native SAF intent | Native Android SAF document tree picker opens | Access state updates, if valid folder selected, returns to Moments with fresh scan | **PASS** | `saf_picker_reconnect.png`, `returned_settings.png` |
| **Settings** | "Private & Local-First" Card (`InkWell`) | Open detailed privacy & security modal | `onTap` -> `KeevaBottomSheet.showTrustDetails(context)` | N/A (Presentation) | N/A | Local UI component | None | Modal sheet slides up explaining zero-network and on-device processing | **PASS** | `settings_privacy_sheet.png` |
| **Modal Sheets** | Drag handle / Backdrop tap / Android Back | Dismiss active bottom sheet | Tap backdrop / swipe down / back press | N/A (Navigator) | N/A | None | None | Sheet dismisses smoothly, underlying screen remains fully interactive | **PASS** | Real device bottom sheet gestures |
